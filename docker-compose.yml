version: '3.8'

services:
  feelem-app:
    image: feelem-springboot-app:latest
    container_name: feelem-spring-boot
    ports:
      - "80:8080"
    environment:
      SPRING_PROFILES_ACTIVE: prod
    volumes:
      - spring_boot_logs:/app/logs
    restart: always

volumes:
  spring_boot_logs:
