version: '3.8'

services:
  feelem-app:
    image: feelem-springboot-app:latest
    container_name: feelem-spring-boot
    ports:
      - "80:8080"
    environment:
      SPRING_PROFILES_ACTIVE: prod
      SPRING_DATASOURCE_URL: 'jdbc:mysql://feelem-db.cbmgucewogo4.ap-northeast-2.rds.amazonaws.com:3306/feelem?sslMode=PREFERRED&allowPublicKeyRetrieval=true&characterEncoding=UTF-8&serverTimezone=UTC'
      SPRING_DATASOURCE_USERNAME: 'feelem'
      SPRING_DATASOURCE_PASSWORD: 'WonderDogs1001'
    volumes:
      - spring_boot_logs:/app/logs
    restart: always

volumes:
  spring_boot_logs:
